# 这个仓库只用 npm 官方源。
#
# 项目级 .npmrc 覆盖用户级的 ~/.npmrc，所以在这个目录里跑的每一条
# npm / npx / pnpm 命令都走官方源，不管机器上全局配的是什么。
#
# 为什么要钉住：镜像站同步比官方慢。刚发完 0.0.2 去核验时，镜像给出的是
# `No matching version found for @nekoleapuki/pamphlet-themes@0.0.2`
# ——包其实已经在官方源上了。那条报错指向的原因是错的，会把人带偏。
#
# 发布更不能走镜像：镜像是只读的，也不认这个账号的登录。
registry=https://registry.npmjs.org/
